---
name: Sync labels
permissions:
  contents: read
  pull-requests: write

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "34 5 * * *"
  workflow_dispatch:

jobs:
  workflows:
    uses: homeassistant-apps/workflows/.github/workflows/labels.yaml@2d9377a1288fbc21f63d7bb263a8f71e46cbdbc7 # main
