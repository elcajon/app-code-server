---
name: PR Labels

# yamllint disable-line rule:truthy
on:
  pull_request_target:
    types:
      - opened
      - labeled
      - unlabeled
      - synchronize

jobs:
  workflows:
    uses: homeassistant-apps/workflows/.github/workflows/pr-labels.yaml@ef730c699b90c59beed86039277f7cbb82928e67 # main
