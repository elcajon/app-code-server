---
name: PR Labels

# yamllint disable-line rule:truthy
on:
  pull_request_target:
    types:
      - opened
      - labeled
      - unlabeled
      - synchronize

jobs:
  workflows:
    uses: homeassistant-apps/workflows/.github/workflows/pr-labels.yaml@2d9377a1288fbc21f63d7bb263a8f71e46cbdbc7 # main
